# Homepage photo-card cache fix

The earlier homepage could show generated placeholder graphics inside some cards because old image files had the same filenames as the new photographs.

This version uses new unique filenames for all seven card photographs and cache-busts styles.css and site.js.

## Replace
- index.html
- upload all seven new files from assets/

You may leave the old asset files in place.
