# Homepage photo cards

All seven homepage cards now use photographs with card-specific coloured overlays.
The typography, symbols, equal-card layout and footer are unchanged.

Replace `index.html` and `styles.css`. Keep the existing `assets/` folder.
