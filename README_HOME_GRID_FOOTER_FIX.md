# Homepage grid and footer repair

This update fixes the layout shown in the screenshot.

## Fixed
- The tall dark action overlays were caused by both `top` and `bottom` being set.
- All eight homepage cards now remain inside a stable equal grid.
- Titles, descriptions and tags stay inside their cards.
- Emergency card typography is contained properly.
- The homepage now has a complete dark dashboard footer.
- Subpage footers receive a matching dark finish.

## Replace
For a focused update, replace only:
- `index.html`
- `styles.css`

The full package is also included.
