# Maps moved under Campus

Everything else remains unchanged.

## Changes
- Removed Maps as a separate homepage card.
- Removed Maps as a top-level navigation and footer item.
- Added Maps clearly inside the Campus card.
- Maps remains available through the Campus page's `Campus map` tab.
- The existing `campus-map.html` interactive page is unchanged.
- Seven homepage cards remain equal-sized.
- Desktop arrangement: four cards in the first row and three centred below.

## Replace
For the focused update, replace:
- `index.html`
- `styles.css`

For consistent navigation across every page, upload the complete package.
