# Homepage header without photograph

Replace these two files in the repository root:

- `index.html`
- `styles.css`

No other HTML page needs to change. The former `home-main-gate.webp` file may remain in `assets/`; it is no longer referenced by the homepage.
